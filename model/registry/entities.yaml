groups:
  - id: entity.eq.character
    type: entity
    name: eq.character
    stability: development
    brief: >
      An EverQuest player character — the entity that telemetry is produced for.
      Its identifying and descriptive attributes are carried as OTLP resource
      attributes on every signal.
    attributes:
      - ref: eq.character.name
        role: identifying
      - ref: eq.character.class
        role: descriptive
      - ref: eq.character.level
        role: descriptive
      - ref: eq.character.deity
        role: descriptive
      - ref: eq.character.aa.unspent
        role: descriptive
